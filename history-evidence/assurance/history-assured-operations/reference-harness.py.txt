# SPDX-License-Identifier: MIT
# Independent specification/reference checks. DOES NOT execute Ada code.
from pathlib import Path
import os, json, struct, hashlib, base64, itertools, copy, tempfile, ctypes, errno, fcntl, subprocess, sys, platform
from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey
from cryptography.hazmat.primitives.serialization import Encoding, PublicFormat
from cryptography.exceptions import InvalidSignature
R=Path('/mnt/data/mcore-harden-work'); E=R/'assurance/evidence'; E.mkdir(exist_ok=True)
F=R/'assurance/tests/fixtures/control-v1'; F.mkdir(parents=True,exist_ok=True)
results=[]
def test(name,kind,fn):
    try:
        fn(); results.append(dict(name=name,kind=kind,result='PASS'))
    except Exception as x:
        results.append(dict(name=name,kind=kind,result='FAIL',detail=repr(x))); raise
H=lambda x:hashlib.sha256(x).digest()
Z=bytes(32); scope=bytes([1])*16; boot=bytes([2])*16; contract=bytes.fromhex((R/'assurance/contract-profile.hex').read_text().strip())
def u64(b,p,v):struct.pack_into('>Q',b,p-1,v)
def writehash(b,start):b[start-1:]=H(b[:start-1]);return bytes(b)
# Deterministic keys ONLY for published test vectors; no private material emitted.
keys=[Ed25519PrivateKey.from_private_bytes(H(b'Mission Core PUBLIC TEST ONLY control fixture '+str(i).encode())) for i in (1,2)]
pubs=[k.public_key().public_bytes(Encoding.Raw,PublicFormat.Raw) for k in keys]
a=bytearray(1024);a[:8]=b'MCROLE01';a[8:24]=scope;a[24:56]=contract;u64(a,57,1);a[64:67]=bytes([2,1,2])
for i in range(2):
    o=96+i*96;a[o:o+32]=pubs[i];a[o+32:o+48]=bytes([3+i])*16;struct.pack_into('>H',a,o+48,i+1);a[o+50]=i
A=writehash(a,993)
s=bytearray(256);s[:8]=b'MCCTL001';s[8:24]=scope;s[24:56]=contract;u64(s,57,1);u64(s,65,1);s[72]=2;s[80:96]=bytes([5])*16;s[96:112]=bytes([5])*16;s[144:176]=bytes([6])*32;s[176:208]=H(A);S=writehash(s,225)
p=bytearray(320);p[:8]=b'MCPROP01';p[8:24]=scope;p[24:56]=contract;u64(p,57,1);u64(p,65,2);p[72]=0;p[80:96]=boot;p[96:112]=bytes([7])*16;p[112:144]=H(S);p[144:176]=bytes([8])*32;p[176:208]=bytes([9])*32;p[208:240]=H(A);u64(p,241,1000);u64(p,249,61000);P=writehash(p,289)
domain=b'MC-CONTROL-v1'; msg=struct.pack('>H',len(domain))+domain+P
sigs=[k.sign(msg) for k in keys];bundle=b''.join(sigs)+bytes(384)
files={'authority.bin':A,'before.bin':S,'proposal.bin':P,'signatures.bin':bundle,'operations.pub':pubs[0],'security.pub':pubs[1],
       'wrong-domain.sig':keys[0].sign(struct.pack('>H',len(domain))+b'MC-CONTROL-v2'+P)}
bad=bytearray(P);bad[73]=1;bad=bytearray(writehash(bad,289));files['reserved.bin']=bytes(bad);files['truncated.bin']=P[:-1]
for name,data in files.items():(F/name).write_bytes(data)
(F/'README.md').write_text('Public deterministic test fixtures only. NEVER provision these public keys as an authority.\nGenerated by an independent Python reference encoder, not by Ada. All private seeds are public-test derivations; no secret is shipped.\nScope, boot, times and receipts are synthetic. Signatures attest only to fixture bytes.\n')
(F/'manifest.sha256').write_text(''.join(f'{H(b).hex()}  {n}\n' for n,b in sorted(files.items())))
def check_lengths():assert (len(A),len(S),len(P),len(bundle))==(1024,256,320,512)
test('canonical-frame-widths','reference',check_lengths)
def check_hashes():
    for b,n in ((A,992),(S,224),(P,288)):assert b[n:]==H(b[:n])
test('canonical-frame-hashes','reference',check_hashes)
def verify_two():
    for i,k in enumerate(keys):k.public_key().verify(bundle[i*64:(i+1)*64],msg)
test('two-independent-ed25519-signatures','crypto-reference',verify_two)
def must_bad(sig,body,key=keys[0]):
    try:key.public_key().verify(sig,body)
    except InvalidSignature:return
    raise AssertionError('invalid signature accepted')
test('domain-substitution-refused','crypto-reference',lambda:must_bad(files['wrong-domain.sig'],msg))
test('wrong-role-key-refused','crypto-reference',lambda:must_bad(sigs[0],msg,keys[1]))
test('tampered-proposal-refused','crypto-reference',lambda:must_bad(sigs[0],msg[:-1]+bytes([msg[-1]^1])))
def canonical_prop(b):
    if len(b)!=320 or b[:8]!=b'MCPROP01' or b[72]>2:return False
    if any(b[73:80]) or any(b[256:288]):return False
    return b[288:]==H(b[:288]) and all(struct.unpack_from('>Q',b,i)[0]<2**63 for i in (56,64,240,248))
test('reserved-fields-refused-despite-rehashed-frame','reference',lambda: (None if not canonical_prop(files['reserved.bin']) else (_ for _ in ()).throw(AssertionError())))
test('truncation-refused','reference',lambda: (None if not canonical_prop(P[:-1]) else (_ for _ in ()).throw(AssertionError())))
def threshold(old,new,votes,epoch,receipt,roles=('ops','sec'),domains=(1,2)):
    # Discrete policy model, not an interpreter of mc_control.adb.
    n=len(set(votes))
    if n<1:return False
    if new<old:return n>=2 and {roles[i] for i in votes}=={'ops','sec'} and len({domains[i] for i in votes})>=2 and epoch>1 and receipt
    return epoch>=1

def threshold_cases():
    for old,new in itertools.product(range(3),repeat=2):
        assert not threshold(old,new,[],2,True)
        assert threshold(old,new,[0],1,False)==(new>=old)
        assert threshold(old,new,[0,1],2,True)
    assert not threshold(2,0,[0,0],2,True)
    assert not threshold(2,0,[0,1],1,True)
    assert not threshold(2,0,[0,1],2,False)
    assert not threshold(2,0,[0,1],2,True,domains=(1,1))
    assert not threshold(2,0,[0,1],2,True,roles=('ops','ops'))
test('latch-all-nine-transitions-and-negative-approvals','reference',threshold_cases)
def permissions():
    for m in range(3):
        for action in range(9):
            got=m==0 or action in (0,1) or (m==1 and action==2)
            assert got==([True]*9 if m==0 else [True,True,True,False,False,False,False,False,False] if m==1 else [True,True,False,False,False,False,False,False,False])[action]
test('interlock-27-operation-cells','reference',permissions)
def anchor_crash():
    # Six durable cuts through object->manifest->pending->anchor->current.
    for cut in range(6):
        old='old';anchor='new' if cut>=4 else old;current='new' if cut>=5 else old;pending='new' if cut>=3 else None
        load=current if current==anchor else None
        reconciled=pending if pending==anchor else None
        if cut<4:assert load==old and reconciled is None
        elif cut==4:assert load is None and reconciled=='new'
        else:assert load=='new' and reconciled=='new'
    assert 'old'!='new' # Equal epoch with a different manifest is not exact-anchor equality.
test('checkpoint-six-durable-cut-reference-states','reference',anchor_crash)
def follows(before,after):
    g,t,e,f,s,c,h=before;G,T,E,F,S,C,H=after
    return G==g+1 and T>=t and E>=e and F>=f and S>=s and C>=c and (C!=c or H==h)
def floors():
    old=(3,4,5,6,7,8,'head');assert follows(old,(4,4,5,6,7,8,'head'))
    for i in range(1,6):
        candidate=[4,4,5,6,7,8,'head'];candidate[i]-=1;assert not follows(old,candidate)
    assert not follows(old,(4,4,5,6,7,8,'other'))
test('checkpoint-no-replay-or-trust-regression','reference',floors)
def chain(c,t):
    seen=[]
    while t is not None:
        if t in seen or t not in c:raise ValueError('cycle/missing parent')
        b=c[t];seen.append(t);pa=b[0]
        if pa is not None:
            if pa not in c or c[pa][2]!=b[1] or c[pa][3]!=b[3]:raise ValueError('gap/lineage')
        t=pa
    h=bytes(32)
    for n in reversed(seen):h=H(b'MCBCH001'+H(n.encode())+h)
    return set(seen),h
C={'a':(None,0,10,'L'),'b':('a',10,20,'L'),'c':(None,0,20,'L')}
def expect_bad(f):
    try:f()
    except ValueError:return
    raise AssertionError('bad input accepted')
def chain_checks():
    members,h=chain(C,'b');assert members=={'a','b'} and h!=chain(C,'c')[1]
    bad=dict(C);bad['b']=('a',11,20,'L');expect_bad(lambda:chain(bad,'b'))
    bad=dict(C);bad['a']=('b',20,10,'L');expect_bad(lambda:chain(bad,'b'))
    bad=dict(C);bad['b']=('a',10,20,'X');expect_bad(lambda:chain(bad,'b'))
    bad=dict(C);del bad['a'];expect_bad(lambda:chain(bad,'b'))
test('backup-chain-closure-cycle-gap-lineage','reference',chain_checks)
def batch_check():
    c={'a':(None,0,10,'L'),'b':(None,0,20,'L'),'c':(None,0,30,'L'),'d':(None,0,40,'L')}
    def allowed(c,delete,minpoints):
        live=set(c)-set(delete)
        return len(live)>=minpoints and all(not(chain(c,t)[0]&set(delete)) for t in live)
    assert allowed(c,['a','b'],2) and not allowed(c,['a','b','c'],2)
    c['d']=('a',10,40,'L');assert not allowed(c,['a'],2)
test('retention-whole-batch-not-independent-items','reference',batch_check)
def clocks():
    lo,hi=1000,1002
    assert not 1001<=lo and not 1001>hi # completion uncertain; receipt expired at high end
    assert 990<=lo and 1100>hi
    assert not 1001<=lo # object lock cannot be assumed ended at lower time bound
    assert 990<=lo
test('bounded-wallclock-conservative-acceptance','reference',clocks)
def recovery_guards():
    facts=dict(anchor=True,isolated=True,epoch=8,old_epoch=7,revision=501,floor=500,caches=True,keys=True)
    ready=lambda d:d['anchor'] and d['isolated'] and d['epoch']>d['old_epoch'] and d['revision']>d['floor'] and d['caches'] and d['keys']
    assert ready(facts)
    for k in ('anchor','isolated','caches','keys'):
        d=dict(facts);d[k]=False;assert not ready(d)
    for k,v in (('epoch',7),('revision',500)):
        d=dict(facts);d[k]=v;assert not ready(d)
test('disaster-old-writers-epoch-cache-revision-guards','reference',recovery_guards)
def double64():
    limit=16384;s=4*((limit+2)//3);j=4*((s+2)//3);assert (s,j)==(21848,29132)
    for n in (1,4096,4097,8192,16384):
        raw=bytes(i%251 for i in range(n));inner=base64.b64encode(raw);outer=base64.b64encode(inner)
        assert len(inner)<=s and len(outer)<=j and base64.b64decode(base64.b64decode(outer))==raw
    assert len(base64.b64encode(bytes(8192)))>8192
    assert 8192>4096
    assert 16385>limit and len(base64.b64encode(bytes(16385)))==s
    # Rounded base64 capacity does not replace the decoded-value length guard.
    assert len(base64.b64encode(bytes(16387)))>s
test('etcd-double-base64-previous-and-new-boundaries','reference',double64)
# Real Linux syscalls, but invoked from Python, NOT the Ada implementation.
root=Path(tempfile.mkdtemp(prefix='mcore-isolated-probes-',dir='/mnt/data/mcore-tools'));os.chmod(root,0o700)
def atomic_conflict():
    lib=ctypes.CDLL(None,use_errno=True);fn=lib.renameat2
    fn.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.c_int,ctypes.c_char_p,ctypes.c_uint];fn.restype=ctypes.c_int
    dfd=os.open(root,os.O_RDONLY|os.O_DIRECTORY)
    try:
        (root/'target').write_bytes(b'old');(root/'staging').write_bytes(b'new')
        for name in ('target','staging'):
            fd=os.open(root/name,os.O_RDONLY);os.fsync(fd);os.close(fd)
        res=fn(dfd,b'staging',dfd,b'target',1);assert res==-1 and ctypes.get_errno()==errno.EEXIST
        os.unlink('staging',dir_fd=dfd);os.fsync(dfd)
        assert (root/'target').read_bytes()==b'old' and not (root/'staging').exists()
    finally:os.close(dfd)
test('linux-noreplace-conflict-staging-cleanup','linux-probe',atomic_conflict)
def lockprobe():
    fd=os.open(root/'lock',os.O_CREAT|os.O_RDWR,0o600);fcntl.flock(fd,fcntl.LOCK_EX|fcntl.LOCK_NB)
    q=subprocess.run([sys.executable,'-c','import os,fcntl,sys; f=os.open(sys.argv[1],os.O_RDWR);\ntry: fcntl.flock(f,fcntl.LOCK_EX|fcntl.LOCK_NB)\nexcept BlockingIOError: sys.exit(0)\nsys.exit(1)',str(root/'lock')],capture_output=True)
    os.close(fd);assert q.returncode==0
test('linux-exclusive-lock-second-process-denied','linux-probe',lockprobe)
def nofollow():
    (root/'link').symlink_to(root/'target')
    try:fd=os.open(root/'link',os.O_RDONLY|os.O_NOFOLLOW)
    except OSError as e:assert e.errno==errno.ELOOP;return
    os.close(fd);raise AssertionError('symlink accepted')
test('linux-final-symlink-nofollow-denied','linux-probe',nofollow)
def durable_replace():
    fd=os.open(root/'next',os.O_CREAT|os.O_EXCL|os.O_WRONLY,0o600);os.write(fd,b'new-state');os.fsync(fd);os.close(fd)
    os.replace(root/'next',root/'current');d=os.open(root,os.O_RDONLY|os.O_DIRECTORY);os.fsync(d);os.close(d)
    assert (root/'current').read_bytes()==b'new-state'
test('linux-private-file-and-directory-fsync','linux-probe',durable_replace)
report={'edition':'assured-operations-2026-09-06','ada_executed':False,'gnatprove_executed':False,'scope':'independent Python specification calculations, Ed25519 fixtures and isolated Linux syscall probes; NOT Ada tests, crash persistence proof or qualification','tests':results,'counts':{k:sum(x['kind']==k for x in results) for k in sorted({x['kind'] for x in results})},'platform':platform.platform(),'harness_sha256':H(Path(__file__).read_bytes()).hex(),'probe_directory':str(root)}
(E/'independent-checks.json').write_text(json.dumps(report,indent=2,ensure_ascii=False)+'\n')
# Archive exact harness as non-runtime evidence; .txt is intentionally not a codebase executable.
(E/'reference-harness.py.txt').write_bytes(Path(__file__).read_bytes())
print(json.dumps(report['counts']));print('total',len(results))
